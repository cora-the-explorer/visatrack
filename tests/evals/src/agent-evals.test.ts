import { describe, it, expect } from 'vitest';

describe('agent evals', () => {
  it.skip('evidence_curator proposes accepted-rate ≥ 0.8 on golden set', () => {
    expect(true).toBe(true);
  });
});
